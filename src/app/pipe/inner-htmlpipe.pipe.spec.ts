import { InnerHTMLPipePipe } from './inner-htmlpipe.pipe';

describe('InnerHTMLPipePipe', () => {
  it('create an instance', () => {
    const pipe = new InnerHTMLPipePipe();
    expect(pipe).toBeTruthy();
  });
});
